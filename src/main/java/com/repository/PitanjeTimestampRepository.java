package com.repository;

import com.model.PitanjeTimestamp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PitanjeTimestampRepository extends JpaRepository<PitanjeTimestamp, Long>
{
}
